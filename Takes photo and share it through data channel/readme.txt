1. in terminal run "node index.js"
2. open "http://localhost:8081/" or "yourip:8081" in browser  (use ip form if opening from other computer connected to same network"
3. Voila you are now connected.Try and send an image.


Note:-  You can open multiple tabs to act as multiple users from same computer.
Note:- Currently this app supports only 2 users.
